# Runtime (see README / Dockerfile: uvicorn app.main:app)
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4
pydantic-settings==2.14.0
websockets==16.0
mcp==1.27.0

# Development / CI (pytest.ini testpaths)
pytest==9.0.3
httpx==0.28.1
